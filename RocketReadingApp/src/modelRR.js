// The system creates a model object for the Rocket Reading system
// Actually, the rocketReadingController object can act as the model object for the system.

var modelRR = {
    this.findNumGamesOfLevel: function () {
        "use strict";    
    }
};